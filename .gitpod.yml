tasks:
  - name: First Running boot ;)
    init: cd web3dev-Solidity_And_Smart_Contracts && yarn       
    command: echo "Rodei 1º ;)"      
    openMode: split-right
    
ports:
  - port: 3000
    onOpen: ignore
    
  - port: 3333
    onOpen: ignore

# sh -c "$(curl -fsSL https://raw.github.com/ohmyzsh/ohmyzsh/master/tools/install.sh) --unattended"
